<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="30dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:layout_marginBottom="20dp"
    tools:ignore="UnusedAttribute">

    <LinearLayout
        android:id="@+id/location_info_ll"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/ubuntu_condensed"
            android:textColor="@color/black"
            android:textSize="24sp"
            tools:text="Mumbai" />

        <TextView
            android:id="@+id/temp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/ubuntu_condensed"
            android:textColor="@color/translucent"
            android:layout_marginVertical="5dp"
            android:textSize="18sp"
            tools:text="22°C" />

        <TextView
            android:id="@+id/description"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/ubuntu_condensed"
            android:textColor="@color/translucent"
            android:textSize="12sp"
            tools:text="Light Drizzle" />

    </LinearLayout>

    <TextView
        android:id="@+id/weather"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:fontFamily="@font/artill_clean_icons"
        tools:text="1"
        android:textColor="@color/black"
        android:textSize="50sp"
        android:gravity="center"
        app:layout_constraintTop_toTopOf="@id/location_info_ll"
        app:layout_constraintBottom_toBottomOf="@id/location_info_ll"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>